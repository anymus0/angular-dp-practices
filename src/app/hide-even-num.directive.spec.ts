import { HideEvenNumDirective } from './hide-even-num.directive';

describe('HideEvenNumDirective', () => {
  it('should create an instance', () => {
    const directive = new HideEvenNumDirective();
    expect(directive).toBeTruthy();
  });
});
